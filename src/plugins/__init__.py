# The only reason we want this file in here is to define this as a
# module that can be imported. This folder will contain plugins that we
# want to have as defaults of the system. The user will define a plugin
# elsewhere outside of the system.
